# Vite Development Server Configuration
VITE_DEV_PORT=5173
VITE_DEV_HOST=localhost

# Vite Preview Server Configuration (for testing production build)
VITE_PREVIEW_PORT=4173
VITE_PREVIEW_HOST=localhost

# Backend API Server Configuration (for proxy)
VITE_API_HOST=localhost
VITE_API_PORT=3000

# Backend Server Configuration (used by Fastify)
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Audio Root Path (optional, defaults to current working directory)
AUDIO_ROOT_PATH=

# Note: Only variables prefixed with VITE_ will be exposed to the client-side code
# Backend variables (PORT, HOST, NODE_ENV, AUDIO_ROOT_PATH) are only available in server code
